const add = (a, b) => a + b;

const subtract = (a, b) => a - b;

const divide = (a, b) => a / b;

const multiply = (a, b) => a * b;

const power = Math.pow;

module.exports = {
  add,
  divide,
  multiply,
  power,
  subtract
};
