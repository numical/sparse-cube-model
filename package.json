{
  "name": "sparse-cube-model",
  "version": "0.0.1",
  "description": "Experiment in scenario-based functional modellng",
  "main": "index.js",
  "dependencies": {
    "debug": "^4.1.1",
    "ramda": "^0.26.1"
  },
  "devDependencies": {
    "faucet": "0.0.1",
    "prettier": "^1.18.2",
    "tap": "^14.6.1"
  },
  "scripts": {
    "test": "tap src/**/*.test.js --100",
    "test:debug": "DEBUG=sparse-cube-model:* tap --only --timeout=600 --no-coverage src/**/*.test.js",
    "test:only": "tap --only --timeout=600 --no-coverage src/**/*.test.js",
    "test:focus": "tap --timeout=600 --no-coverage src/**/Model.serialization.test.js",
    "test:model": "tap --timeout=600 --no-coverage src/**/Model.**.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/numical/sparse-cube-model.git"
  },
  "keywords": [
    "functional",
    "mode"
  ],
  "author": "Mike Evans (mike.evans at numical.com)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/numical/sparse-cube-model/issues"
  },
  "homepage": "https://github.com/numical/sparse-cube-model#readme"
}
